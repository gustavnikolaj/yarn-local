#!/usr/bin/env node

require = require("esm")(module); // eslint-disable-line no-global-assign
const wrap = require("@gustavnikolaj/async-main-wrap");
const main = require("../lib/cli.js").default;

wrap(main)(process.cwd(), process.argv.slice(2));
